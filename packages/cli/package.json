{
  "name": "@tempque/cli",
  "version": "1.0.0",
  "description": "Agent-first CLI for TempQue temporary email service",
  "type": "module",
  "bin": {
    "tempque": "dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target=node",
    "dev": "bun run ./src/index.ts"
  },
  "files": ["dist", "skill"],
  "keywords": ["email", "temporary", "cli", "agent", "ai"],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@tempque/core": "file:../core",
    "@types/node": "^20.0.0"
  }
}
